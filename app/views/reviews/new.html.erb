

<%= simple_form_for [@attendance, @review] do |f| %>
  <%= f.input :comment, placeholder: "How was your run?", label: "Leave a Review" %>
  <%= f.input :rating, placeholder: "Rate the run", label: false %>
  <%= f.submit class: "btn btn-outline-primary" %>
<% end %>
